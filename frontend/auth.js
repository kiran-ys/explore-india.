const panels={login:document.querySelector("#loginForm"),register:document.querySelector("#registerForm")},status=document.querySelector("#authStatus");
document.querySelectorAll("[data-panel]").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll("[data-panel]").forEach(item=>item.classList.toggle("active",item===button));Object.entries(panels).forEach(([key,form])=>form.hidden=key!==button.dataset.panel);status.hidden=true}));
async function submit(form,endpoint){const button=form.querySelector("button"),data=Object.fromEntries(new FormData(form));button.disabled=true;try{const response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)}),result=await response.json();if(!response.ok)throw new Error(result.error);location.href=result.data.role==="admin"?"admin.html":"states.html"}catch(error){status.textContent=error.message||"Unable to continue.";status.classList.add("error");status.hidden=false;button.disabled=false}}
panels.login.addEventListener("submit",event=>{event.preventDefault();submit(panels.login,"/api/auth/login")});panels.register.addEventListener("submit",event=>{event.preventDefault();submit(panels.register,"/api/auth/register")});
