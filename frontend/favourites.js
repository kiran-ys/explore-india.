const savedGrid=document.querySelector("#savedGrid"),savedEmpty=document.querySelector("#savedEmpty"),savedIntro=document.querySelector("#savedIntro");
const regionNames={north:"North India",south:"South India",east:"East India",west:"West India",central:"Central India",northeast:"Northeast India"};
function savedCard(item){const card=document.createElement("article"),image=document.createElement("img"),content=document.createElement("div"),region=document.createElement("span"),title=document.createElement("h3"),summary=document.createElement("p"),remove=document.createElement("button");card.className="destination-card";image.src=item.imagePath;image.alt=item.imageAlt;image.loading="lazy";region.textContent=regionNames[item.region];title.textContent=item.name;summary.textContent=item.summary;remove.className="favourite-button saved";remove.type="button";remove.dataset.removeFavourite=item.id;remove.setAttribute("aria-label",`Remove ${item.name} from saved destinations`);remove.textContent="♥ Saved";content.append(region,title,summary);card.append(image,content,remove);return card}
async function loadSaved(){const session=await fetch("/api/auth/me").then(response=>response.json());if(!session.data){window.location.replace("auth.html");return}savedIntro.textContent=`${session.data.name.split(" ")[0]}, these are the places you want to remember.`;const response=await fetch("/api/favourites");if(!response.ok)throw new Error("Could not load saved destinations");const result=await response.json();savedGrid.replaceChildren(...result.data.map(savedCard));savedEmpty.hidden=result.count!==0}
savedGrid.addEventListener("click",async event=>{const button=event.target.closest("[data-remove-favourite]");if(!button)return;button.disabled=true;const response=await fetch(`/api/favourites/${button.dataset.removeFavourite}`,{method:"DELETE"});if(response.ok)loadSaved();else{button.disabled=false;button.textContent="Try again"}});loadSaved().catch(()=>{savedIntro.textContent="We could not load your saved destinations. Please try again."});
