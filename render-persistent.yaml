services:
  - type: web
    name: explore-india-kiran
    runtime: docker
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
    disk:
      name: explore-india-data
      mountPath: /app/data
      sizeGB: 1
